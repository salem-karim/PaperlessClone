spring:
  application:
    name: REST-API
  datasource:
    url: jdbc:postgresql://localhost:5432/paperless
    username: paperless
    password: secret
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
